# ============================================================================
# Claude Security Reviewer v2.1.0 - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ============================================================================

# -- API CREDENTIALS (REQUIRED) --
# Choose ONE of the following providers:

# Anthropic Claude API
ANTHROPIC_API_KEY=sk-ant-v7-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OR OpenAI API
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -- OPTIONAL: Custom API Endpoints --
# For using OpenRouter, LocalAI, or enterprise proxies
API_BASE_URL=https://api.openrouter.io/api/v1
API_PROVIDER=openai  # or 'anthropic', 'custom'

# -- MODEL SELECTION --
CLAUDE_MODEL=claude-opus-4-1-20250805
OPENAI_MODEL=gpt-4o

# -- WEB SERVER CONFIGURATION --
HOST=127.0.0.1
PORT=8095
RELOAD=false  # Set to 'true' only during development

# -- SECURITY: API Token for Web Server --
# REQUIRED in production - generates a Bearer token for API access
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# API_TOKEN=your-secure-random-token-here

# -- ALLOWED HOSTS (for CORS) --
ALLOWED_HOSTS=localhost,127.0.0.1

# -- LOGGING --
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# -- TIMEOUTS (seconds) --
GITHUB_API_TIMEOUT=30
LLM_TIMEOUT=300
GIT_CLONE_TIMEOUT=60

# -- EXCLUSIONS --
# Comma-separated list of directories to exclude from scanning
EXCLUDE_DIRECTORIES=node_modules,venv,.git,tests,docs

# -- FEATURE FLAGS --
ENABLE_CACHE=true
CACHE_TTL_DAYS=7

# -- GITHUB ACTIONS (auto-filled in CI/CD) --
# These are automatically provided by GitHub Actions
# GITHUB_REPOSITORY=owner/repo
# GITHUB_TOKEN=ghs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GITHUB_PR_NUMBER=123
